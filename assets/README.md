## Kim Jong-un Photo Assets

- Add one or more photo files of Kim Jong-un to this folder, e.g. `kim-photo-1.jpg`.
- Keep file names free of spaces or special characters so the browser can load them easily.
- Update `script.js` or use the file picker in the UI to select the photo you want to turn into a puzzle.
